# [PLAN] 캐릭터 엔티티 능력치 제한 요구사항 필드 추가

- **작성일**: 2026-03-09
- **작성자**: Gemini CLI (bkit)
- **목표**: CharacterEntity에 장비 착용을 위한 능력치 제한(요구사항) 필드 14종을 추가한다.

## 1. 요구사항 분석
- 특정 장비(벨트, 전낭, 배낭, 탈것 등) 착용에 필요한 능력치 요구사항을 저장할 필드 필요.
- 제공된 14종의 제한 사양을 `String` 타입으로 추가.
- `accessory4` 필드 아래에 삽입.
- `@Schema`의 `description`에 원본 텍스트와 카테고리(벨트/전낭, 배낭/탈것) 명시.

## 2. 작업 상세
- [ ] `CharacterEntity.java`에 요구사항 필드 14종 추가
- [ ] 각 필드에 JPA `@Column(length = 100)` 적용
- [ ] 각 필드에 Swagger `@Schema` 적용 (원본 텍스트 반영)

## 3. 기대 결과
- 캐릭터의 장비 착용 제한 정보를 세밀하게 관리할 수 있음.
- 시스템적으로 능력치에 따른 장비 장착 가능 여부 판단의 기초 데이터로 활용 가능.
